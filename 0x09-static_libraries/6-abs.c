#include "main.h"
/**
 * _abs - to get abslute value.
 * @n: integer
 * Return: return the abslute value of n.
 */
int _abs(int n)
{
  if (n >= 0)

        {

                return (n);

        }

        return (-n);
}
